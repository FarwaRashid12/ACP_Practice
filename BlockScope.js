//Create an if statement and create a block-level variable( now you have to decide which variable type you will use here).


    if (true) {
        let blockVar = "I am inside an if block";
        console.log(blockVar); // Accessible inside the block
    }

    // console.log(blockVar); //  Error: blockVar is not accessible here

